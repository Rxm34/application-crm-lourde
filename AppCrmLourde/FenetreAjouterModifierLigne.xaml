<Window x:Class="AppCrmLourde.FenetreAjouterModifierLigne"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ligne de Facture" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Produit :" Grid.Row="0" FontWeight="Bold" Margin="0,0,0,5"/>
        <ComboBox x:Name="ProduitComboBox" Grid.Row="1" Height="30" Margin="0,0,0,15"/>

        <TextBlock Text="Quantité :" Grid.Row="2" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="QteTextBox" Grid.Row="3" Height="30" Margin="0,0,0,15" VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Annuler" Width="80" Height="30" Margin="0,0,10,0" Background="#9E9E9E" Foreground="White" Click="BtnAnnuler_Click"/>
            <Button Content="Valider" Width="80" Height="30" Background="#4CAF50" Foreground="White" FontWeight="Bold" Click="BtnValider_Click"/>
        </StackPanel>
    </Grid>
</Window>