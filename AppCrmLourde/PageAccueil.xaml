<Page x:Class="AppCrmLourde.PageAccueil"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Accueil - AppCrmLourde"
      Width="1280" Height="720"
      Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="400"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding TodayText}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="AgendaGrid" Margin="0,0,0,20"/>
        </ScrollViewer>

        <UniformGrid Grid.Row="2" Rows="1" Columns="6" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0" Width="1100" Height="100" >

            <UniformGrid.Resources>
                <Style TargetType="Button">
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="Margin" Value="8,0"/>
                    <Setter Property="Background" Value="#2C3E50"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                        </Setter.Value>
                    </Setter>
                </Style>
            </UniformGrid.Resources>

            <Button Content="Prospects/Clients" Click="BtnPage1_Click"/>
            <Button Content="Rendez-vous" Click="BtnPage2_Click"/>
            <Button Content="Produits" Click="BtnPage3_Click"/>
            <Button Content="Factures" Click="BtnPage4_Click"/>
            <Button Content="Dashboard" Click="BtnPage5_Click"/>
            <Button x:Name="BtnPageAdmin" Content="Admin" Click="BtnPage6_Click"/>

        </UniformGrid>
    </Grid>
</Page>